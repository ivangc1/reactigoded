# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencias
node_modules/
.pnp
.pnp.js

# Builds
dist/
dist-ssr/
*.local

# state.css fragments — autogenerados por scripts/build-state-css-fragments.mjs
# en cada `npm run build`. NO editar a mano; si cambia el state.css fuente
# regenera y commitea solo el cambio de origen, no los fragmentos.
src/styles/state/

# Storybook
storybook-static/

# Editor
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tests
coverage/

# Vite cache
.vite/

# RC1 baseline snapshots (local-only)
.rc1-baseline/

# Claude Code session data (prompts, transcripts internos).
# Excluido para evitar leak accidental por git add -A.
.claude/

# Notas y scripts locales del autor (no API pública del repo).
# Cubre patrones .notes-betaXX.txt, .release-betaXX.sh, BLOQUEOS.md,
# SESION-RESUMEN*.md. El autor decide si moverlos fuera del repo.
.notes-*
.release-*
.pr-body-*.md
BLOQUEOS.md
SESION-RESUMEN*.md

# Env
.env
.env.local
.env.*.local

# OS
Thumbs.db

# Local snapshots de scripts/config de la VPS — viven en igoded@server, no en el paquete reactigoded
deploy*.sh*
deploy-worker*.sh*
igoded.es.*nginx*
webhook.php*
*.tgz
*.tgz
