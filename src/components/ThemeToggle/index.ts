export { ThemeToggle } from "./ThemeToggle";
export type { ThemeToggleProps } from "./ThemeToggle";
// `Theme` se re-exporta desde el root (`reactigoded`) vía `hooks/useTheme`
// — fuente única de verdad para evitar colisión de nombres.
